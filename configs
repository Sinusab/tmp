nextflow.enable.dsl = 2

includeConfig 'conf/base.config'
includeConfig 'conf/modules.config'
// includeConfig 'conf/slurm.config'   ← فعلاً کامنت یا حذف
// includeConfig 'conf/test.config'    ← فعلاً لازم نیست
