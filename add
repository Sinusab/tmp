// Ensure processes run in a login-like bash so `module` is available
process {
  shell = ['/bin/bash', '-lc']
}

// Load R inside the SLURM job for the ASCNA step
process {
  withName: 'CALL_ASCN' {
    beforeScript = 'module load r-light/4.4.1'
  }
}
