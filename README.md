## Why Singularity instead of Conda?

- Offline HPC environment
- Conda not available on compute nodes
- Need for reproducibility

## Why BioContainers instead of self-built image?

- Conda solver instability on HPC
- BioContainers provides tested, pinned builds
